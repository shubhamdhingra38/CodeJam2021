#include <bits/stdc++.h>
using namespace std;

void Solve(int t) {}

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    int t;
    cin >> t;
    // t = 1;
    for (int i = 1; i <= t; ++i) {
        Solve(i);
    }
}